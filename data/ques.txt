Create a table that has three columns (name (char(1)), roll (integer), and cgpa (fraction(3,2))) and set name as the primary key.
Insert one row. value of this row (a, 12, 7.7).
Rename the attribute name (name to stu_name).
Change the column definition (char to varchar).
Add the column "dept_name".
Remove the column ("dept_name").